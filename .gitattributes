# Normalize text files and enforce sane line endings across platforms.
#
# Notes:
# - Git stores text files with LF in the repository; `eol=` controls checkout.
# - We enforce LF for POSIX/Cygwin shell tooling and checksum lists (to avoid `\r`
#   breaking parsers like `sha256sum -c`).
# - Windows batch/cmd scripts are checked out with CRLF for best compatibility.

# Default: auto-detect text; leave binaries untouched.
* text=auto

# Repo meta files
.gitattributes text eol=lf
.gitignore text eol=lf
.editorconfig text eol=lf

# ------------------------------------------------------------------------------
# Scripts (LF)
# ------------------------------------------------------------------------------
*.sh text eol=lf
*.pl text eol=lf
*.py text eol=lf
*.cmake text eol=lf
CMakeLists.txt text eol=lf

# Scripts without extensions
scripts/build-helpers text eol=lf
scripts/genini text eol=lf

# Patches/diffs/checksums should stay LF
*.patch text eol=lf
*.diff text eol=lf
**/osgeo4w/patch text eol=lf
**/osgeo4w/diff text eol=lf
*.sha256sum text eol=lf
*.sha512 text eol=lf

# Common text templates/lists
*.tmpl text eol=lf
*.lst text eol=lf
*.hint text eol=lf
*.vars text eol=lf

# ------------------------------------------------------------------------------
# Windows command scripts (CRLF)
# ------------------------------------------------------------------------------
*.bat text eol=crlf
*.cmd text eol=crlf

# ------------------------------------------------------------------------------
# Binary files (never modify, never diff)
# ------------------------------------------------------------------------------
*.exe binary
*.dll binary
*.ico binary
*.bmp binary
*.msi binary
*.7z binary
*.zip binary
*.gz binary
*.bz2 binary
*.xz binary
*.tar binary
*.tgz binary
*.tbz2 binary
*.txz binary
